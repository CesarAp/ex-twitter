<div class="row">
    <div class="col-xs-12 col-sm-6 col-sm-offset-3 profile">
        <h3 class="pull-left"><strong>@<%= user.username %></strong></h3>
        <% if (typeof(session) != 'undefined' && session.username !== user.username ) { %>
            <form action="/profile/<%= user.username %>/follow" class="pull-right" method="post">
                <div class="form-actions">
                    <button class="btn btn-primary" type="submit"><%= following ? 'Unfollow' : 'Follow' %></button>
                </div>
            </form>
        <% } %>
        <div class="clearfix"></div>
        <hr>
    </div>
</div>
<%- include('../tweets/partials/list', { tweets: tweets }); %>
